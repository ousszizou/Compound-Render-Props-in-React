import { defineSemanticTokens } from "@pandacss/dev";

export const animations = defineSemanticTokens.animations({
  "indeterminate-bar": {
    value: "indeterminate-bar 1.5s cubic-bezier(0.65, 0.815, 0.735, 0.395) infinite"
  }
});
